# nixpacks.toml
nixPkgs = ["python39", "opencv", "mesa", "mesa_glu", "libglvnd", "glibc"]

[phases.build]
cmds = [
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "gunicorn api:app --bind 0.0.0.0:$PORT"